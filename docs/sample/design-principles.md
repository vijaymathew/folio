# Design Principles

## Colours

Use restrained accents against a dark neutral base so widgets stay readable in the terminal.

Prefer a small number of semantic colours:
- primary for focus and primary actions
- warning for advisories and risky operations
- error for failures and blocked states

## Typography

Keep labels short and use emphasis sparingly so dense documents remain scannable.

